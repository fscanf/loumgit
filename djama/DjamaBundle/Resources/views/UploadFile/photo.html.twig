<!DOCTYPE html>
<html>
    <head>
        {% block title %}
            <title>{{title}}</title>
        {% endblock %}
    </head>
    <body>
            <p>test photo</p>
            <form action="{{path('djama_photo')}}" method="post" {{form_enctype(form)}}>
                <p>
                <h3>Téléchargement du fichier</h3>
                 {{form_errors(form.photo)}}
                 {{form_label(form.photo, 'Photo')}}
                 {{form_widget(form.photo, {'attr':{'placeholder': 'Photo' }}) }}

                 <input type="submit"    name="form1" value="Sauver"/>
                </p>
                {{form_rest(form)}}
            </form>